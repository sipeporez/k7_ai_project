package com.motorPM.controller;

import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;

import com.motorPM.domain.Member;
import com.motorPM.service.LoginService;

import lombok.RequiredArgsConstructor;

@Controller
@RequiredArgsConstructor
public class LoginController {
	
	private final LoginService ls;
	
//	@PostMapping("/test")
//	public ResponseEntity<T>
//	
	
//	@PostMapping("/login")
//	public ResponseEntity<?> login(@RequestBody Member m) {
//		System.out.println("login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨login 호출됨");
//		if (ls.login(m) != null) return ResponseEntity.ok().body("로그인 성공");
//		else return ResponseEntity.status(HttpStatus.UNAUTHORIZED).body("로그인 실패");
//	}
}
