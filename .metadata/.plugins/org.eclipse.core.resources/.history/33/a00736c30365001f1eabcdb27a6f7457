package com.motorPM.controller;

import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.motorPM.domain.Member;
import com.motorPM.service.LoginService;
import com.motorPM.service.PMFlaskService;

import lombok.RequiredArgsConstructor;


@Controller
@RequiredArgsConstructor
public class PMFlaskController {
	private final PMFlaskService pmfs;
	
	
	@GetMapping("/flask")
	public ResponseEntity<?> flaskTest() throws JsonProcessingException {
		return ResponseEntity.ok(pmfs.flasktest()); 
	}
	
	@GetMapping("/")
	public ResponseEntity<?> index() {
		return ResponseEntity.ok("인덱스"); 
	}

}
